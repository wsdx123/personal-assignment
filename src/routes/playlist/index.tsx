import styles from './playlist.module.scss'

const Playlist = () => {
  return (
    <div className={styles.wrapper}>
      <div className={styles.player}>playlist</div>
    </div>
  )
}

export default Playlist
